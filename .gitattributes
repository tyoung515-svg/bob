# Convergence board §3 rule 3 (2026-07-18): LF everywhere on the CachyOS trees.
# Auto-detected binaries are untouched by text=auto.
* text=auto eol=lf

# cmd.exe requires CRLF in batch files
*.bat text eol=crlf
*.cmd text eol=crlf
