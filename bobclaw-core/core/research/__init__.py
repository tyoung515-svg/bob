"""BoBClaw research lane (MS2-R*) — orchestration + retrieval for the deep-research lane.

MS2-R1 lives here: ``retrieve.py`` (the LKS-first research retriever — own corpus first, web second).
Additive package; consumed by the CitationAgent (R4) as the entailment gate's ``retrieve`` callable.
"""
