[pytest]
asyncio_mode = auto
pythonpath = .
addopts = --disable-socket --allow-hosts=127.0.0.1,::1,localhost
markers =
    integration: requires live external services (e.g. Qdrant); excluded from default collection
